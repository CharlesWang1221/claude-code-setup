# 2026-08-17 學習日記

## 今天做了什麼
把《不標準答案》網站的集數、網誌、影音三個列表頁改成統一的卡片版型，一排 2 張為基準，手機自動收成 1 排。原本三個頁面各寫一套樣式（集數是條列 list、網誌是左邊框卡、影音是自動填滿的 grid），這次拆掉各自的寫法，統一改用共用的 `.cards-2col` + `.media-card`，寫進 `global.css` 讓三頁共用同一套視覺語言，不用各自維護一份 CSS。

測試影音頁版型時，順手發現一個 UX 缺口：導覽列完全沒有標示目前在哪一頁，切來切去容易迷路。在 `Base.astro` 加了 `isActive()`，用 `Astro.url.pathname` 判斷目前頁面，該項目會亮白字＋加粉紫漸層底線。

## 效果
- 集數／網誌／影音三頁視覺一致，改完用瀏覽器截圖核對過：卡片間距、hover 效果正常，縮小視窗會自動收成單排。
- 導覽列現在切到哪一頁都看得出來，解決了「不知道自己在哪」的問題。
- 改動已經 build 過（54 頁全過）、commit 到 `agent/sync-mac-github-updates`（`041f873`），還沒 push。

## 反思
導覽列沒有 active 狀態這個 bug，不是我主動去找的，是邊做邊測邊比對才順手抓到。提醒自己：改完一個頁面的樣式，最好順手把整站導覽走一輪，不要只盯著自己改的那個頁面看。

## 下一步
- [ ] 決定要不要把這批改動 push 上 GitHub
- [ ] Codex 同時在跑 s3ep3 的內容和影片，等它做完再看要不要一起處理、一起 push

---

## YouTube OAuth 授權排錯

### 遇到什麼問題

換到 Mac 後，`tools/youtube-upload.js` 缺少 `tools/client_secret.json` 與 `tools/youtube-token.json`，因此無法使用 YouTube 自動上傳。

一開始照文件進 Google Cloud Console 下載憑證，卻一直看到「需要額外存取權」。當時最容易誤判成「還沒登入」，實際檢查頁面右上角帳號後，才發現瀏覽器裡的 2 個帳號都沒有 `podcast-tools-501708` 專案權限。光貼同一個專案網址，不會改變登入身分，也不代表帳號真的有權限。

### 怎麼查

沒有繼續在登入頁打轉，而是先查本機下載資料夾。結果找到 `client_secret.json.json`，這才是剛下載完成的檔案，只是 Chrome 因副檔名重複，多補了一次 `.json`。

接著只檢查 JSON 結構，不輸出秘密內容。確認它是 Desktop OAuth Client、`project_id` 正確、包含 Client ID、Client Secret 與 `http://localhost` redirect URI，再複製成工具預期的 `tools/client_secret.json`。

### 排除過程

憑證放好後仍沒看到授權畫面，原因不是 Google 壞掉，而是原本的上傳腳本必須先收到 `--episode`，而且找到該集的 `youtube.txt` 與 `.mp4`，才會走到 OAuth。為了避免「只想授權，卻意外開始上傳影片」，這次沒有硬跑完整上傳流程，而是單獨產生只有 `youtube.upload` scope 的授權網址。

Google 授權完成後跳到 `http://localhost`，Chrome 顯示 `ERR_CONNECTION_REFUSED`。這個畫面看起來像失敗，其實授權碼已經放在網址列。工具沒有啟動 localhost callback server，所以瀏覽器當然連不上；真正要做的是取回一次性 code，再向 Google Token API 交換 token。

第一次交換 token 時又遇到 `getaddrinfo ENOTFOUND oauth2.googleapis.com`。這不是授權碼失效，而是執行環境的網路沙箱擋住 DNS。允許該次 Google Token API 連線後重跑，成功產生 `tools/youtube-token.json`，內含 access token 與 refresh token。

### 最後怎麼驗證

- `client_secret.json` 的專案 ID 與 OAuth 類型正確。
- `youtube-token.json` 同時有 access token、refresh token，scope 僅限 `youtube.upload`。
- 兩個檔案權限都是 `600`，只有本機使用者能讀。
- 兩個檔案都被 `.gitignore` 排除，沒有進入公開 repo。
- 授權過程沒有上傳任何影片。

### 這次學到的

OAuth 排錯要把 4 層拆開看：Cloud 專案權限、Client 憑證、瀏覽器使用者授權、token 交換。只盯著「有沒有登入」會在錯的樓層繞圈。

看到 localhost 拒絕連線也別急著判定失敗。先看網址有沒有 `code`，再判斷是 OAuth 被拒絕，還是根本沒有人在本機接 callback。這次真正的 bug 不是 Google 授權失敗，是舊腳本把「取得授權」綁死在「準備上傳某一集」後面，讓第一次設定很難單獨驗證。

---

## S3EP3 內容製作與社群發布

### 今天完成的內容

把 S3EP3 從逐字稿整理推進到可對外使用的內容包。核准的節目標題是「當 AI 跑到你前面，你真的還擁有那份作品嗎？」，核心主張是 AI 可以代做成品，卻不能替人補上學習、判斷與真實活過的創作歷程。

完成節目介紹、Show Notes、YouTube 說明、FB 長文與 IG 文案，並通過品牌閘門。文案維持心維空間是母品牌、《不標準答案》是旗下產品；沒有把 AI 寫成敵人，也沒有用「再不用 AI 就落後」這類焦慮話術逼人選邊。

整理並核准 5 句原話金句，其中節目預告主句是：「你沒有真的擁有它。AI 跑到你的前面了，你只是假裝拿著他的作業去交。」

完成 S3EP3 的 IG／一般平台節目預告。兩個版本的差異固定下來：只有 IG 可以出現「Show Notes 連結在 bio」，其他平台不得放這句。片尾統一補上節目收聽位置。

完成 7 張 IG 金句輪播：1 張主題封面、5 張核准金句、1 張收聽資訊。收聽頁依既有版型固定為「完整集數／在 Firstory 等你／Show Notes 連結在 bio／Firstory・Spotify・Apple／《不標準答案》S3EP3」。

輪播最後已發布到 Instagram，並開啟 Facebook 同步。正確的 IG 貼文連結是：`https://www.instagram.com/p/DcJFApikRBC/`。

SEO 文章也已完成，題目與集數主張一致；今天沒有把私人原始音檔、家庭素材、Google Drive 絕對路徑或帳號憑證提交到公開 repo。

### 今天新增或確認的規則

Podcast 預告不能為了湊秒數切斷一句話。先依逐字稿找完整語意，再用靜音偵測確認停頓；句尾保留約 0.3 至 0.8 秒。如果自然斷點超過原定片長，影片跟著延長。

今天抽出的 5 段金句音檔都依這個規則處理。第 10 段第一次多吃到下一句約 0.39 秒，重新往前修到自然停頓內，證明只看文字時間碼不夠，音檔尾端仍要做靜音與實聽檢查。

社群金句輪播沿用既有格式：同一張節目靜態插圖做不同人物裁切，疊深色遮罩、橘色識別線與大字原話。每頁只放一個觀點，不把 5 句塞在同一張圖裡。

IG 輪播的驗收不能看圖片內自己寫的 `1/7`，也不能只看本機有 7 個檔案。發布前必須在 Instagram 裁切頁看到 7 個實際縮圖與 7 個輪播圓點；發布後還要打開貼文，確認右側有「下一步」按鈕。

### 剪紙短片做到哪裡

完成第 5 句剪紙動態 proof，以及 AI 跑者、作業紙、空手等分層素材。字幕框改成圓角、半透明深底，字體加大，並增加「本集金句」標示；片尾加入完整節目與收聽平台資訊。

音檔已加入完整版本，時間軸設計為前段作業被 AI 帶走、後段拿回作業蓋上「代交」、最後進收聽片尾。不過正式渲染前停下，因為現有素材黑線、紙張顆粒和人物比例太粗，已經搶過金句本身。

剪紙短片其餘 4 支暫停，不因為 proof 已做就硬著頭皮批量生產。下一輪要先重新定義素材質感，做 1 張靜態風格稿確認，再恢復動畫。

### 遇到的問題與修正

1. HyperFrames 完整版開場一度沒有顯示作業紙與手。原因是後段 `fromTo` 的 `immediateRender` 提前覆蓋了初始狀態，已在後段動畫補上 `immediateRender: false`。
2. 最後一次 HyperFrames 檢查需要連 npm registry，環境 DNS 回 `ENOTFOUND`，所以沒有把未完成的剪紙版本冒充正式成品。已保留程式、音檔與素材，等待新視覺方向。
3. 本機 Puppeteer 在沙箱內啟動 Chromium 時被 macOS 的 Mach port 權限擋住。改用已安裝的 Playwright，經授權在沙箱外輸出 1080 × 1080 圖片。
4. 第一版收聽頁被共用的 `grayscale(1)` 一起套用，藍色漸層變成灰色。CTA 背景改成獨立樣式並覆寫 `filter: none`，同時把文案改回既有的固定格式。
5. 第一次上傳 IG 時，我把圖片上的 `1/7` 誤認成平台已建立 7 張輪播，實際只發布了 1 張。這是驗收錯誤，不是 Instagram 顯示問題。
6. 發現錯誤後刪除單張貼文，重新從裁切頁逐張加入第 2 至第 7 張。這次先用畫面確認 7 個縮圖與圓點，發布後再確認貼文出現「下一步」按鈕，才判定完成。

### 今天最重要的學習

畫面裡寫著「完成」不等於平台狀態真的完成。影片要驗音軌與自然停頓，輪播要驗平台縮圖與切換按鈕，發布要驗最終網址；每一層都需要平台本身的證據。

這次 IG 單張誤發的根因，是拿內容層的 `1/7` 當成系統層的「7 張已加入」。之後發布驗收必須分開看：檔案數、編輯器素材數、發布後可切換數，三個數字要一致。

### 下次接手

- [ ] 先決定剪紙素材要往更細緻線稿、低顆粒紙雕，還是更扁平的拼貼圖形走。
- [ ] 新方向先做 1 張靜態風格稿，不直接做完整動畫。
- [ ] 風格核准後再完成第 5 支正式渲染，接著才批次做其餘 4 支。
- [ ] 補做一次 Facebook 同步貼文的公開頁驗收，確認 7 張圖片與分享對象都符合預期。
- [ ] 整理今天工作區的未提交檔案；公開 repo 只提交可公開的程式、規則與文案，不提交瀏覽器 session、原始音檔或私人素材。

### 待討論的渠道分流想法

老查提出一個尚未定案的方向：FB 的精華影片使用剪紙效果，IG 的精華影片改用漫畫效果。目的是讓兩個平台不只是裁切比例不同，而是各自有可辨認的視覺語言。

下次要討論三件事：兩套風格是否都能服從同一套品牌準則、每集同時製作兩種版本的時間成本，以及 FB／IG 受眾是否真的需要不同敘事節奏。討論完成前，不把這個想法寫成正式製作規則，也不直接批量執行。
