# DESIGN.md — 老查 / 《不標準答案》品牌設計系統

> 給 AI 讀的品牌說明書，跟 `CLAUDE.md`（你是誰、怎麼協作）互補：`CLAUDE.md` 講協作方式，這份講視覺長什麼樣。
> 幫忙做圖卡、簡報、Landing Page 前，先讀這份，不要憑空猜配色。
> 內容是從現有的網站 CSS 和 `/cards` skill 模板實際色碼整理出來的，不是憑空定義——改動請同步回原始檔案，不要只改這份文件。

---

## 品牌不是單一套色系，是兩套各自服務不同場景

老查目前有兩套視覺系統，刻意分開，不要互相套用：

| 系統 | 用途 | 色系 |
| :-- | :-- | :-- |
| **網站（Basie）** | 《不標準答案》官網、部落格、影片頁 | 深紫夜光 |
| **IG 圖卡（/cards）** | 社群貼文、輪播、金句圖 | 藍黑系 / 橘白系（雙選） |

---

## 系統一：網站（Basie 深紫夜光）

來源：`site/src/styles/global.css`、`site/src/layouts/Base.astro`

### 色彩

| 用途 | 色碼 | 說明 |
| :-- | :-- | :-- |
| 主色（強調/CTA） | `#815BDB` | 紫，按鈕、連結、重點文字 |
| 亮點色 | `#EE359E` | 桃紅，用於少量高對比強調（標題底線、hover） |
| 亮點色 2 | `#AADD22` | 螢光綠黃，極少量點綴用，不要大面積 |
| 背景深 1 | `#0F0235` | 最深層背景 |
| 背景深 2 | `#15083A` | 次層背景 |
| 背景深 3 | `#1A0A45` | 卡片/區塊背景 |
| 背景深 4 | `#2A1A5E` | 更淺一層的區塊背景 |
| 文字（淺紫） | `#CAC0E4` | 次要文字、說明文字 |
| 文字（灰） | `#AAA8BD` | 更淡的輔助文字 |
| 文字（白） | `#FFFFFF` | 主要標題、正文 |

### 字體

- 標題／中文正文：`Noto Sans TC`, `PingFang TC`, `Heiti TC`, `Microsoft JhengHei`, sans-serif
- Mono（程式碼、標籤、數字）：`SF Mono`, `Consolas`, `Courier New`, monospace

### 風格關鍵字

深色系、夜光感、紫色為主的科技溫度感；桃紅跟螢光綠黃只做點綴，不要當底色；避免用網站的紫粉配色去做 IG 圖卡（會跟現有的 /cards 兩套配色衝突，兩邊要分開）。

---

## 系統二：IG 圖卡（/cards skill，兩套可選）

來源：`~/.claude/skills/social-cards/assets/{blue-dark,orange-light}/cover.html`

### 🔵 藍黑系（深色科技感）

| 用途 | 色碼 |
| :-- | :-- |
| 背景深 1 | `#01101A` |
| 背景深 2 | `#0A2A35` |
| 強調色（主） | `#4AB897`（青綠） |
| 強調色（亮） | `#6DE3EA`（淺青） |
| 文字 | `#FFFFFF` |

適合：教學、工具介紹、技術分享、逐字稿知識點。

### 🟠 橘白系（明亮溫暖）

| 用途 | 色碼 |
| :-- | :-- |
| 深色文字/元素 | `#40444D` |
| 強調色 | `#E27F2E`（橘） |
| 背景 | 淺色（白/近白，模板內以淺底為主） |
| 文字（深色底上） | `#FFFFFF` |

適合：觀點分享、心得、金句、生活風格內容。

### 字體

`Inter`（英文/數字）+ `Noto Sans TC`（中文），sans-serif。

### 風格關鍵字

一張圖卡 = 一個重點；標題最多 10 字，內文 3-4 行，條列最多 4 項。版型固定四種：cover / content-text / content-image / cta，不要自創新版型。

---

## 給 AI 的判斷原則

1. **網站相關**（首頁、部落格頁、影片頁、Landing Page 如果是掛在 podcast 品牌下）→ 用系統一（Basie 紫粉深色）
2. **IG / 社群圖卡**（`/cards` 產出）→ 用系統二，藍黑或橘白由用戶當次選擇，不要混用兩套配色在同一組圖卡裡
3. **新場景不確定用哪套** → 先問用戶這是「網站延伸」還是「社群貼文」，不要自己猜、也不要生第三套新色系
4. **顏色以這份文件為準**，如果之後 `global.css` 或 `/cards` 模板改了顏色，要回來同步更新這份文件

---

## Do's and Don'ts

- ✅ 深色背景 + 少量高對比強調色
- ✅ 中文一律用 Noto Sans TC 系列，不要換成預設無襯線字體
- ✅ 兩套 IG 配色維持獨立，各自的四種版型內部一致
- ❌ 不要用漸層背景（現有系統都是純色/深淺分層，不是漸層）
- ❌ 不要幫網站生出第三種主色，紫粉系統只有這 4 個強調色可用
- ❌ 不要把網站的紫色系用在 IG 圖卡上，或把 IG 的藍黑/橘白用在網站上
